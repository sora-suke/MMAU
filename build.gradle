buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath "net.minecraftforge.gradle:ForgeGradle:1.2-SNAPSHOT"
    }
}
 
apply plugin: "forge"
 
sourceSets {
    main {
        java { srcDirs = [ "$projectDir/java" ] }
        resources { srcDirs = [ "$projectDir/resource" ] }
    }
}
 
tasks.withType(Jar) {
		compileApiJava.options.encoding = 'UTF-8'
		compileJava.options.encoding = 'UTF-8'
} 

archivesBaseName = "MoreMaterialsAndUtilities"
version = "0.1.1-1.7.10"
minecraft.version = "1.7.10-10.13.4.1614-1.7.10"
 
[compileJava, compileTestJava].each {
    it.options.encoding = 'UTF-8'
    it.options.compilerArgs += ['-source', '1.7', '-target', '1.7']
}